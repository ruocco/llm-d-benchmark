{# Harness pod template for benchmark execution.
   This template is rendered programmatically by step_04_run_experiment.py
   with runtime values (endpoint URL, experiment ID, results directory).
   It is NOT rendered during the plan phase.
#}
apiVersion: v1
kind: Pod
metadata:
  name: {{ pod_name }}
  namespace: {{ harness.namespace | default(namespace.name) }}
  labels:
    app: {{ harness.podLabel }}
    function: load_generator
spec:
  containers:
  - name: harness
    image: {{ images.benchmark.repository }}:{{ images.benchmark.tag }}
    imagePullPolicy: {{ images.benchmark.pullPolicy }}
{% if harness.runAsUser is defined and harness.runAsUser is not none %}
    securityContext:
      runAsUser: {{ harness.runAsUser }}
{% endif %}
    command: ["sh", "-c"]
    args:
    - >-
      for script in /workspace/harnesses/*; do
        [ -f "$script" ] || continue;
        cp "$script" /usr/local/bin/;
        chmod +x "/usr/local/bin/$(basename "$script")";
      done;
      {{ harness_command }}
    resources:
      limits:
        cpu: "{{ harness.resources.cpu }}"
        memory: {{ harness.resources.memory }}
      requests:
        cpu: "{{ harness.resources.cpu }}"
        memory: {{ harness.resources.memory }}
    env:
    - name: LLMDBENCH_RUN_EXPERIMENT_LAUNCHER
      value: "1"
    - name: LLMDBENCH_MAGIC_ENVAR
      value: "harness_pod"
    - name: LLMDBENCH_DEPLOY_METHODS
      value: "{{ deploy_method | default('modelservice') }}"
    - name: LLMDBENCH_CLUSTER_TYPE
      value: "{{ cluster_type | default('unrecognized') }}"
    - name: LLMDBENCH_RUN_WORKSPACE_DIR
      value: "{{ experiment.workspaceDir }}"
    - name: LLMDBENCH_CONTROL_WORK_DIR
      value: "{{ results_dir }}"
    - name: LLMDBENCH_DEPLOY_CURRENT_MODEL
      value: "{{ model.name }}"
    - name: LLMDBENCH_DEPLOY_CURRENT_TOKENIZER
      value: "{{ model.name }}"
    - name: LLMDBENCH_HARNESS_STACK_ENDPOINT_URL
      value: "{{ endpoint_url }}"
    - name: LLMDBENCH_HARNESS_STACK_TYPE
      value: "{{ stack_type }}"
    - name: LLMDBENCH_HARNESS_NAME
      value: "{{ harness.name }}"
    - name: LLMDBENCH_RUN_EXPERIMENT_ID
      value: "{{ experiment_id }}"
    - name: RAYON_NUM_THREADS
      value: "{{ harness.inferencePerf.rayonNumThreads }}"
    - name: HOME
      value: "{{ harness.resultsDirPrefix | default('/requests') }}/.home"
    - name: HF_HOME
      value: "{{ harness.resultsDirPrefix | default('/requests') }}/.cache/huggingface"
    - name: XDG_CACHE_HOME
      value: "{{ harness.resultsDirPrefix | default('/requests') }}/.cache"
    - name: MPLCONFIGDIR
      value: "{{ harness.resultsDirPrefix | default('/requests') }}/.cache/matplotlib"
{% if huggingface.enabled %}
    - name: HF_TOKEN_SECRET
      value: "{{ huggingface.secretName }}"
    # HuggingFace harness binaries split between the two env-var
    # names: guidellm and a number of vLLM utilities read HF_TOKEN,
    # while huggingface_hub-based libraries also accept
    # HUGGING_FACE_HUB_TOKEN.  Setting both from the same secretKeyRef
    # eliminates the question -- any harness that reads either name
    # gets the token.
    - name: HF_TOKEN
      valueFrom:
        secretKeyRef:
          name: {{ huggingface.secretName }}
          key: {{ huggingface.tokenKey }}
          # optional: offline / external-endpoint harnesses start without the secret.
          optional: true
    - name: HUGGING_FACE_HUB_TOKEN
      valueFrom:
        secretKeyRef:
          name: {{ huggingface.secretName }}
          key: {{ huggingface.tokenKey }}
          optional: true
{% endif %}
    - name: POD_NAME
      valueFrom:
        fieldRef:
          fieldPath: metadata.name
    - name: POD_UID
      valueFrom:
        fieldRef:
          fieldPath: metadata.uid
    - name: LLMDBENCH_VLLM_COMMON_NAMESPACE
      value: "{{ namespace.name }}"
{% if base64_context_contents is defined and base64_context_contents %}
    - name: LLMDBENCH_BASE64_CONTEXT_CONTENTS
      value: "{{ base64_context_contents }}"
{% endif %}
    - name: LLMDBENCH_VLLM_COMMON_INFERENCE_PORT
      value: "{{ vllmCommon.inferencePort }}"
    - name: LLMDBENCH_RUN_EXPERIMENT_RESULTS_DIR_PREFIX
      value: "{{ harness.resultsDirPrefix | default('/requests') }}"
    - name: LLMDBENCH_HARNESS_STACK_NAME
      value: "{{ harness.stackName | default(model_id_label) | default('default') }}"
{% if monitoring is defined and monitoring.metricsScrapeEnabled is defined %}
    - name: LLMDBENCH_VLLM_COMMON_METRICS_SCRAPE_ENABLED
      value: "{{ monitoring.metricsScrapeEnabled | lower }}"
    - name: LLMDBENCH_VLLM_COMMON_METRICS_PORT
      value: "{{ decode.vllm.port }}"
    - name: LLMDBENCH_VLLM_MONITORING_METRICS_PATH
      value: "{{ monitoring.metricsPath }}"
    - name: LLMDBENCH_EPP_METRICS_SECRET
      value: "{{ router.monitoring.secretName | default('inference-gateway-sa-metrics-reader-secret') }}"
{% if monitoring.timeSeriesMetrics is defined %}
    - name: LLMDBENCH_TIME_SERIES_METRICS
      value: >-
        {{ monitoring.timeSeriesMetrics | tojson }}
{% endif %}
{% if monitoring.embedTimeSeries is defined %}
    - name: LLMDBENCH_METRICS_EMBED_TIME_SERIES
      value: "{{ monitoring.embedTimeSeries | lower }}"
{% endif %}
{% if monitoring.timeSeriesMaxPoints is defined %}
    - name: LLMDBENCH_METRICS_TS_MAX_POINTS
      value: "{{ monitoring.timeSeriesMaxPoints }}"
{% endif %}
{% endif %}
    - name: LLMDBENCH_STANDALONE_ENABLED
      value: "{{ standalone.enabled | lower }}"
    - name: LLMDBENCH_STANDALONE_LAUNCHER_ENABLED
      value: "{{ standalone.launcher.enabled | lower }}"
    - name: LLMDBENCH_FMA_ENABLED
      value: "{{ fma.enabled | lower }}"
    - name: LLMDBENCH_VLLM_COMMON_VLLM_LOAD_FORMAT
      value: "{{ standalone.vllm.loadFormat }}"
{% if standalone.enabled and standalone.launcher.enabled %}
    - name: LLMDBENCH_VLLM_STANDALONE_LAUNCHER_PORT
      value: "{{ standalone.launcher.port }}"
    - name: LLMDBENCH_VLLM_STANDALONE_LAUNCHER_VLLM_PORT
      value: "{{ standalone.launcher.vllmPort }}"
{% endif %}
{% if fma.enabled %}
    - name: LLMDBENCH_FMA_LAUNCHER_CONFIG_PORT
      value: "{{ fma.launcherConfigurator.port }}"
    - name: LLMDBENCH_FMA_ITERATIONS
      value: "{{ fma.iterations }}"
    - name: LLMDBENCH_FMA_MAX_INSTANCES
      value: "{{ fma.launcher.maxInstances }}"
{% endif %}
{% if harness.extraEnvVars is defined and harness.extraEnvVars %}
{% for env_entry in harness.extraEnvVars %}
    - name: {{ env_entry.name }}
      value: "{{ env_entry.value }}"
{% endfor %}
{% endif %}
    volumeMounts:
    - name: results
      mountPath: /requests
{% set profile_mounts = profile_mounts | default([harness.name]) %}
{% for profile_harness in profile_mounts %}
    - name: {{ profile_harness }}-profiles
      mountPath: /workspace/profiles/{{ profile_harness }}
{% endfor %}
    - name: harness-scripts
      mountPath: /workspace/harnesses
  volumes:
  - name: results
    persistentVolumeClaim:
      claimName: {{ storage.workloadPvc.name }}
{% for profile_harness in profile_mounts %}
  - name: {{ profile_harness }}-profiles
    configMap:
      name: {{ profile_harness }}-profiles
{% endfor %}
  - name: harness-scripts
    configMap:
      name: llmdbench-harness-scripts
      defaultMode: 0755
{% if vllmCommon.pullSecret is defined and vllmCommon.pullSecret %}
  imagePullSecrets:
  - name: {{ vllmCommon.pullSecret }}
{% endif %}
{% if harness.serviceAccount is defined and harness.serviceAccount %}
  serviceAccountName: {{ harness.serviceAccount }}
{% endif %}
{% if harness.nodeSelector is defined and harness.nodeSelector %}
  nodeSelector:
{% for key, value in harness.nodeSelector.items() %}
    {{ key }}: "{{ value }}"
{% endfor %}
{% endif %}
{% if harness.tolerations is defined and harness.tolerations %}
  tolerations:
{% for t in harness.tolerations %}
  - key: {{ t.key }}
{% if t.operator is defined %}
    operator: {{ t.operator }}
{% endif %}
{% if t.value is defined %}
    value: "{{ t.value }}"
{% endif %}
{% if t.effect is defined %}
    effect: {{ t.effect }}
{% endif %}
{% endfor %}
{% endif %}
  restartPolicy: Never
