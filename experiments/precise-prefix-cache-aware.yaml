# =====================================================================
# Experiment: Precise Prefix-Cache-Aware Routing Workload Matrix
# =====================================================================
#
# Evaluates how prefix group count and system prompt length affect
# performance under different prefix-cache-aware routing strategies.
# The setup phase selects the routing plugin (default round-robin,
# estimate-based, or tracking-based prefix scoring); the run phase
# sweeps workload characteristics to measure how well each routing
# strategy exploits prefix cache locality.
#
# Usage (full DoE — automatic standup/run/teardown per setup treatment):
#   llmdbenchmark --spec guides/precise-prefix-cache-routing experiment \
#     --experiments experiments/precise-prefix-cache-aware.yaml
#
# Usage (run-only — targets an already-running endpoint):
#   llmdbenchmark --spec guides/precise-prefix-cache-routing run \
#     --endpoint-url http://<HOST>:<PORT> \
#     --model <MODEL_NAME> \
#     --namespace <NAMESPACE> \
#     --harness inference-perf \
#     --workload shared_prefix_synthetic.yaml \
#     --experiments experiments/precise-prefix-cache-aware.yaml
#
# The experiment form auto-detects the endpoint from stood-up stacks
# and reads harness/profile defaults from the plan config.
# The run-only form requires explicit --endpoint-url, --model, and
# --namespace since there is no stood-up stack to detect from.
#
# =====================================================================

# --- DoE Metadata (informational — not consumed by the runtime) ------

experiment:
  name: precise-prefix-cache-aware
  description: >
    Full factorial sweep of prefix group count and system prompt length
    to compare routing strategies for prefix-cache-aware scheduling.
    Measures how accurately each routing plugin steers requests to
    replicas that already have the relevant prefix cached.
  harness: inference-perf
  profile: shared_prefix_synthetic.yaml

design:
  type: full_factorial

  setup:
    factors:
      - name: pluginsConfigFile
        key: router.epp.pluginsConfigFile
        levels: [default, prefix-cache-estimate-config, prefix-cache-tracking-config]
        description: Routing plugin configuration for prefix-cache-aware scheduling
    constants:
      - key: model.maxModelLen
        value: 16000
      - key: model.blockSize
        value: 64

  run:
    factors:
      - name: num_groups
        key: data.shared_prefix.num_groups
        levels: [40, 60]
        description: >
          Number of distinct prefix groups.  Higher group counts create
          more contention for prefix cache slots across replicas.

      - name: system_prompt_len
        key: data.shared_prefix.system_prompt_len
        levels: [8000, 5000, 1000]
        unit: tokens
        description: >
          Length of the shared system prompt per group.  Longer prompts
          make cache hits more valuable (more tokens reused).

    constants:
      - key: api.streaming
        value: true
        description: Streaming inference enabled
      - key: data.shared_prefix.num_prompts_per_group
        value: 32
        description: Prompts per prefix group (held fixed)
      - key: data.shared_prefix.question_len
        value: 256
        description: Per-request question length (held fixed)
      - key: data.shared_prefix.output_len
        value: 256
        description: Per-request output length (held fixed)

  total_setup_treatments: 3
  total_run_treatments: 6
  total_matrix: 18

  response_variables:
    - TTFT (Time to First Token)
    - TPOT (Time per Output Token)
    - ITL (Inter-Token Latency)
    - E2EL (End-to-End Latency)
    - Throughput (tokens/sec)
    - Prefix cache hit rate (from vLLM metrics)
    - Request routing distribution across replicas

# --- Runtime: Setup treatments (consumed by experiment orchestrator) -------

setup:
  constants:
    model.maxModelLen: 16000
    model.blockSize: 64
  treatments:
    # router.epp.pluginsConfigFile flows through 12_router-values.yaml.j2
    # to the llm-d-router chart's `router.epp.pluginsConfigFile` value.
    - name: routing-default
      router.epp.pluginsConfigFile: default-plugins.yaml
    - name: routing-estimate
      router.epp.pluginsConfigFile: prefix-cache-estimate-config.yaml
    - name: routing-tracking
      router.epp.pluginsConfigFile: prefix-cache-tracking-config.yaml

# --- Treatments (consumed by step_04 render_profiles) ----------------
#
# Full factorial: 2 levels of num_groups x 3 levels of system_prompt_len
# = 6 treatments.
#
# Naming: grp{num_groups}-splen{system_prompt_len_in_k}k
#
# Note: The original experiment defined a fractional design with only
# 3 treatments.  This has been expanded to the full factorial to
# enable estimation of interaction effects between group count and
# prompt length.

treatments:
  # --- num_groups = 40 (fewer prefix groups) ---
  - name: grp40-splen8k
    data.shared_prefix.num_groups: 40
    data.shared_prefix.system_prompt_len: 8000

  - name: grp40-splen5k
    data.shared_prefix.num_groups: 40
    data.shared_prefix.system_prompt_len: 5000

  - name: grp40-splen1k
    data.shared_prefix.num_groups: 40
    data.shared_prefix.system_prompt_len: 1000

  # --- num_groups = 60 (more prefix groups) ---
  - name: grp60-splen8k
    data.shared_prefix.num_groups: 60
    data.shared_prefix.system_prompt_len: 8000

  - name: grp60-splen5k
    data.shared_prefix.num_groups: 60
    data.shared_prefix.system_prompt_len: 5000

  - name: grp60-splen1k
    data.shared_prefix.num_groups: 60
    data.shared_prefix.system_prompt_len: 1000
